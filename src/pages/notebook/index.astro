---
import BaseLayout from "../../layouts/BaseLayout.astro";

const allNotebooks = await Astro.glob("./*.md");

const pageTitle = "Notebook";
---

<BaseLayout pageTitle={pageTitle} subNavPages={allNotebooks}/>