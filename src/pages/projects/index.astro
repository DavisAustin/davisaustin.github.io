---
import BaseLayout from "../../layouts/BaseLayout.astro";

const allProjects = await Astro.glob("./*.md");

const pageTitle = "Projects";
---

<BaseLayout pageTitle={pageTitle} subNavPages={allProjects}>