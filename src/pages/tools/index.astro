---
import BaseLayout from "../../layouts/BaseLayout.astro";

const allTools = await Astro.glob("./*.md");

const pageTitle = "Tools";
---

<BaseLayout pageTitle={pageTitle} subNavPages={allTools}></BaseLayout>