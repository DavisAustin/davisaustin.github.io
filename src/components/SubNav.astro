---
const { title } = Astro.props;
---

<aside class="flex flex-col w-40 border-r border-border-default p-1">
  <header class="px-4 py-2 mt-1">
    <span class="text-base font-bold">{title}</span>
  </header>
 <slot />
</aside>